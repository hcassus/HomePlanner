machine:
  java:
    version: openjdk8
    
  services:
    - postgresql

database:
  override:
    - createdb hrp

deployment:
  staging:
    branch: master
    heroku:
      appname: nameless-brushlands-59783



test:
  script: "mvn cobertura:cobertura"
  post:
    - bash <(curl -s https://codecov.io/bash)
