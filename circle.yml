machine:
  java:
    version: openjdk8
    
  services:
    - postgresql

database:
  override:
    - createdb hrp

deployment:
  staging:
    branch: master
    heroku:
      appname: powerful-ravine-24650